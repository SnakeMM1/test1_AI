# test1_AI

Python project that generates random coloured pyramids in the terminal.

## Run

```bash
python3 color_pyramids.py
```

The script asks how many pyramids to generate. Each pyramid gets:
- a random size
- a random RGB colour
